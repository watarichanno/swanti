[project]
name = "swanti"
version = "0.1.0"
description = "Scripts for the Southern World Assembly Initiative program of the South Pacific"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "google-api-python-client>=2.177.0",
    "networkx>=3.5",
    "requests>=2.32.4",
]

[dependency-groups]
dev = ["ruff>=0.12.4"]
